<ol class="post-list">
  {{#each posts as |post|}}
  <li>
   {{> post_info post }}
  </li>
  {{/each}}
</ol>

<div>
  <ul class= "categories">
    {{#each categories as |category|}}
    <li><a href= "{{category.category_name}}">{{category.category_name}}</a></li>
    {{/each}}
  </ul>
</div>